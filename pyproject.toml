[project.scripts]
dev = "personal_box.main:main"

[tool.uv]
package = true

[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "personal-box"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "jinja2>=3.1.6",
    "msal>=1.35.1",
    "openai>=2.26.0",
    "pydantic>=2.12.5",
    "python-dotenv>=1.2.2",
    "python-multipart>=0.0.22",
    "pyyaml>=6.0.3",
    "requests>=2.32.5",
    "uvicorn[standard]>=0.41.0",
]
